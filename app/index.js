import { useState } from "react";
import { View, Text, SafeAreaView, ScrollView } from "react-native";

const Home = () => {
  return (
    <View>
      <Text> Home </Text>
    </View>
  );
};

export default Home;
